# Phase 4: Портфельный слой

## Краткое описание

Реализован портфельный слой поверх существующих стратегий (RR, RRD, Runner) для реалистичной симуляции торговли с учетом комиссий, проскальзывания и портфельных ограничений.

## Основные изменения

### 🎯 Новые возможности

1. **Портфельный движок** (`backtester/domain/portfolio.py`)
   - Единый баланс в SOL для всех стратегий
   - Динамическое/фиксированное управление размером позиций
   - Учет комиссий (swap, LP, network) и проскальзывания (10% по умолчанию)
   - Портфельные ограничения:
     - Максимальная экспозиция (max_exposure)
     - Максимальное количество открытых позиций (max_open_positions)
   - Backtest window (ограничение по датам)
   - Equity curve (кривая баланса)

2. **Интеграция в BacktestRunner**
   - Автоматический запуск портфельной симуляции после выполнения стратегий
   - Парсинг конфигурации портфеля из YAML
   - Сохранение результатов по каждой стратегии

3. **Расширенная отчетность**
   - Портфельные отчеты (CSV, JSON, графики)
   - Equity curve визуализация
   - Детальная статистика по позициям
   - Предупреждения в Strategy-level отчетах о различиях в метриках

### 📁 Новые файлы

- `backtester/domain/portfolio.py` - портфельный движок (~330 строк)
- `docs/PORTFOLIO_LAYER.md` - документация портфельного слоя
- `docs/PNL_CALCULATION_EXPLANATION.md` - объяснение различий в расчете PnL

### 🔧 Измененные файлы

1. **`backtester/domain/position.py`**
   - `meta` теперь использует `field(default_factory=dict)` вместо `None`
   - Упрощает работу с портфельными метаданными

2. **`backtester/application/runner.py`**
   - Добавлен метод `_build_portfolio_config()` - парсинг конфигурации из YAML
   - Добавлен метод `run_portfolio()` - запуск портфельной симуляции
   - Добавлен атрибут `self.portfolio_results` для хранения результатов

3. **`backtester/infrastructure/reporter.py`**
   - Добавлен метод `save_portfolio_results()` - сохранение портфельных результатов
   - Добавлен метод `plot_portfolio_equity_curve()` - построение графика equity curve
   - Добавлены предупреждения в Strategy-level отчеты о различиях в метриках

4. **`config/backtest_example.yaml`**
   - Добавлена секция `backtest` с `start_at` и `end_at` (опционально)
   - Добавлена секция `portfolio` с настройками:
     - `initial_balance_sol` - начальный баланс
     - `allocation_mode` - режим аллокации (fixed/dynamic)
     - `percent_per_trade` - доля капитала на сделку
     - `max_exposure` - максимальная экспозиция
     - `max_open_positions` - максимум открытых позиций
     - `fee` - модель комиссий и проскальзывания

5. **`main.py`**
   - Добавлен вызов `runner.run_portfolio()` после `runner.run()`
   - Автоматическое сохранение портфельных результатов

6. **`CHANGELOG.md`**
   - Добавлена секция Phase 4 с подробным описанием всех изменений

## Исправления багов

1. **Исправлена логика баланса в портфельном движке**
   - При открытии позиции: баланс уменьшается на размер позиции
   - При закрытии позиции: баланс увеличивается на размер + PnL
   - Ранее баланс не изменялся при открытии позиций

2. **Исправлен расчет экспозиции**
   - Теперь правильно учитывается, что баланс уменьшается при открытии позиций
   - Экспозиция рассчитывается от общего капитала (баланс + открытые позиции)

3. **Исправлена обработка backtest window**
   - Добавлена обработка случаев, когда секция `backtest` отсутствует или имеет значение `None`
   - Добавлена сортировка equity curve по времени для корректного расчета drawdown

## Конфигурация

### Пример конфигурации портфеля

```yaml
backtest:
  # start_at: "2024-06-01T00:00:00Z"  # Опционально
  # end_at: "2024-07-01T00:00:00Z"    # Опционально

portfolio:
  initial_balance_sol: 10.0
  allocation_mode: "dynamic"      # "fixed" или "dynamic"
  percent_per_trade: 0.1          # 10% на сделку
  max_exposure: 0.5               # Максимум 50% капитала в рынке
  max_open_positions: 10
  fee:
    swap_fee_pct: 0.003           # 0.3%
    lp_fee_pct: 0.001             # 0.1%
    slippage_pct: 0.10            # 10%
    network_fee_sol: 0.0005       # 0.0005 SOL
```

## Результаты

После реализации портфельного слоя система теперь:

✅ **Реалистично симулирует торговлю:**
- Учитывает все комиссии и проскальзывание
- Применяет портфельные ограничения
- Генерирует equity curve

✅ **Гибко настраивается:**
- Через YAML конфигурацию
- Разные режимы аллокации (fixed/dynamic)
- Настраиваемые лимиты и комиссии

✅ **Полная отчетность:**
- Equity curve в CSV и PNG
- Детальная информация по позициям
- Статистика портфеля

✅ **Автоматическая интеграция:**
- Запускается автоматически после выполнения стратегий
- Работает со всеми стратегиями без изменений в их коде

## Сохраняемые файлы

Для каждой стратегии создаются:
- `{strategy_name}_equity_curve.csv` - кривая баланса
- `{strategy_name}_portfolio_positions.csv` - все портфельные позиции
- `{strategy_name}_portfolio_stats.json` - статистика портфеля
- `{strategy_name}_portfolio_equity.png` - график equity curve

## Важные замечания

### Различия в метриках

- **Strategy-level Total PnL** - это сумма процентов от отдельных сделок (например, 15% + 15% = 30%)
- **Portfolio-level Total return** - это реальная доходность портфеля с учетом размеров позиций и динамического баланса (например, 2.85%)

**Для оценки реальной прибыльности используйте Portfolio-level отчеты!**

Подробное объяснение: см. `docs/PNL_CALCULATION_EXPLANATION.md`

## Тестирование

1. Запустите бэктест:
   ```bash
   python main.py
   ```

2. Проверьте вывод:
   - Должны быть видны результаты портфельной симуляции
   - Должны создаться файлы в `output/reports/`

3. Проверьте equity curve:
   - Откройте `output/charts/{strategy_name}_portfolio_equity.png`
   - Проверьте, что баланс изменяется при открытии/закрытии позиций

## Breaking Changes

Нет. Все изменения обратно совместимы. Портфельная симуляция запускается автоматически, но не влияет на существующие Strategy-level отчеты.

## Будущие улучшения

- [ ] Режим Runner-XN (закрытие всего портфеля при достижении XN любой позицией)
- [ ] Более сложные модели комиссий
- [ ] Учет частичного закрытия позиций
- [ ] Портфельная оптимизация

## Связанные документы

- `docs/PORTFOLIO_LAYER.md` - полная документация портфельного слоя
- `docs/PNL_CALCULATION_EXPLANATION.md` - объяснение различий в расчете PnL
- `CHANGELOG.md` - история изменений

---

**Дата:** 2025-01-XX  
**Фаза:** Phase 4  
**Тип:** Feature (новый функционал)




