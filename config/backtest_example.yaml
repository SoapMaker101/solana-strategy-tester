# config/backtest_example.yaml
# Глобальные настройки бэктеста (будем развивать на следующих фазах)

data:
  loader: "gecko"         # "csv" или "gecko"
  candles_dir: "data/candles/cached"
  timeframe: "1m"
  before_minutes: 60
  after_minutes: 360


report:
  output_dir: "output/reports"

runtime:
  max_signals: 100000
  parallel: false  # на будущее
