# config/backtest_example.yaml
# 📊 Глобальные настройки для запуска бэктеста

backtest:
  # start_at: "2024-06-01T00:00:00Z"  # Начало окна бэктеста (опционально, закомментировано = без ограничений)
  # end_at: "2024-07-01T00:00:00Z"    # Конец окна бэктеста (опционально, закомментировано = без ограничений)

data:
  loader: "gecko"                 # Источник данных: "csv" (локальный файл) или "gecko" (онлайн через API GeckoTerminal)
  candles_dir: "data/candles/cached"  # Папка для хранения/поиска свечных данных (используется при csv и gecko кешировании)
  timeframe: "15m"                # Таймфрейм свечей: поддерживаются "1m" (минутные) и "15m" (агрегированные по 15 минут)
  before_minutes: 60             # Кол-во минут ДО сигнала, которые нужно загрузить (например, для входа по просадке)
  after_minutes: 43200           # Кол-во минут ПОСЛЕ сигнала (43200 минут = 30 дней) — максимальная длина позиции

portfolio:
  initial_balance_sol: 10.0       # Начальный баланс в SOL
  allocation_mode: "dynamic"      # "fixed" или "dynamic" (размер позиции от начального/текущего баланса)
  percent_per_trade: 0.1          # Доля капитала на одну сделку (10%)
  max_exposure: 0.5               # Максимальная экспозиция портфеля (50% капитала в рынке)
  max_open_positions: 10          # Максимальное количество одновременно открытых позиций
  runner_reset_enabled: false     # Режим Runner-XN (пока не реализован)
  fee:
    swap_fee_pct: 0.000          # Комиссия swap (0.3%)
    lp_fee_pct: 0.000             # Комиссия LP (0.1%)
    slippage_pct: 0.0            # Проскальзывание (10%)
    network_fee_sol: 0.000       # Фиксированная комиссия сети в SOL

report:
  output_dir: "output/reports"   # Папка, куда сохраняются отчёты с результатами бэктеста (.json на стратегию)

runtime:
  max_signals: 100000            # Ограничение на максимальное количество сигналов в одном запуске
  parallel: false                # (на будущее) — флаг параллельной обработки сигналов
