# Кеширование и тестирование производительности

## 📦 Задача 1: Улучшенное кеширование в GeckoTerminalPriceLoader

### Что изменилось

Метод `load_prices()` теперь использует умное кеширование:

1. **Проверка кеша** - перед загрузкой через API проверяется наличие кеша
2. **Проверка покрытия диапазона** - если кеш покрывает нужный временной интервал полностью, используется только кеш (без API запросов)
3. **Дозагрузка** - если диапазон не покрыт, загружаются недостающие данные через API
4. **Логирование** - подробные сообщения о состоянии кеша

### Логи кеширования

```
[CACHE ✅] Using cached candles for {contract} - диапазон полностью покрыт кешем
[CACHE ⚠️] Incomplete coverage, fetching missing candles for {contract} - нужна дозагрузка
[CACHE ❌] No cache found, fetching full candles for {contract} - кеша нет, полная загрузка
```

### Пример использования

```python
from backtester.infrastructure.price_loader import GeckoTerminalPriceLoader
from datetime import datetime, timezone, timedelta

loader = GeckoTerminalPriceLoader(
    cache_dir="data/candles/cached",
    timeframe="15m"
)

# Первый вызов - загрузит через API и сохранит в кеш
start = datetime.now(timezone.utc) - timedelta(days=1)
end = datetime.now(timezone.utc)
candles1 = loader.load_prices("3N8WSNyCqosL26dftn7Mysma4K3ZmmZGDNpzjNSLpump", start, end)
# [CACHE ❌] No cache found, fetching full candles...

# Второй вызов с тем же диапазоном - использует только кеш
candles2 = loader.load_prices("3N8WSNyCqosL26dftn7Mysma4K3ZmmZGDNpzjNSLpump", start, end)
# [CACHE ✅] Using cached candles for ...
```

### Структура кеша

```
data/candles/cached/
  └── {contract_address}_{timeframe}.csv
```

Формат CSV:
```csv
timestamp,open,high,low,close,volume
2025-12-10T12:00:00+00:00,0.001,0.0012,0.0009,0.0011,1000000
...
```

### Преимущества

- ✅ **Снижение нагрузки на API** - повторные запросы используют кеш
- ✅ **Быстрая обработка** - нет задержек при работе с кешированными данными
- ✅ **Устойчивость** - при ошибках API можно использовать кеш как fallback
- ✅ **Прозрачность** - подробное логирование состояния кеша

### TODO (будущие улучшения)

- [ ] Дозагрузка только недостающих частей (начало или конец диапазона)
- [ ] Слияние кеша с новыми данными вместо полной перезаписи
- [ ] Настройка TTL для кеша
- [ ] Сжатие старых кешей

---

## 🧪 Задача 2: Тестирование производительности

### Скрипт tests/test_performance.py

Скрипт для тестирования производительности `BacktestRunner` при разных настройках параллельности.

### Запуск тестов

```bash
python tests/test_performance.py
```

### Что тестируется

1. **Тест №1: Последовательный режим**
   - `parallel = False`
   - Базовый тест для сравнения

2. **Тест №2: Параллельный режим (4 потока)**
   - `parallel = True`
   - `max_workers = 4`
   - Проверка умеренной параллельности

3. **Тест №3: Параллельный режим (10 потоков)**
   - `parallel = True`
   - `max_workers = 10`
   - Проверка агрессивной параллельности

### Метрики

Скрипт собирает следующие метрики:

- **Время выполнения** - общее время выполнения бэктеста
- **Количество сигналов** - всего обработано
- **Успешных результатов** - количество успешных сделок
- **Ошибок** - количество ошибок при обработке
- **Нет входа** - количество сигналов без входа
- **Среднее время на сигнал** - производительность на один сигнал

### Пример вывода

```
🚀 Запуск тестов производительности BacktestRunner
============================================================

🧪 ТЕСТ: Тест №1: Последовательный режим
============================================================
Параметры:
  - parallel: False
  - max_workers: 1
  - signals: signals/test_signals.csv

📩 Загружено сигналов: 7
[CACHE ✅] Using cached candles for ...
...

✅ Тест завершен успешно
📊 Метрики:
  - Время выполнения: 45.23 сек
  - Всего сигналов: 7
  - Успешных результатов: 5
  - Ошибок: 0
  - Нет входа: 2
  - Среднее время на сигнал: 6.46 сек

============================================================
📊 СВОДНАЯ ТАБЛИЦА РЕЗУЛЬТАТОВ
============================================================
Тест                                    Время (сек)     Сигналов     Успешно    Ошибок    
------------------------------------------------------------
Тест №1: Последовательный режим        45.23          7            5          0         
Тест №2: Параллельный режим (4 потока) 12.15          7            5          0         
Тест №3: Параллельный режим (10 потоков) 8.42         7            5          0         

============================================================
⚡ СРАВНЕНИЕ ПРОИЗВОДИТЕЛЬНОСТИ
============================================================
Последовательный режим: 45.23 сек
Параллельный (4 потока): 12.15 сек
Ускорение: 3.72x
Параллельный (10 потоков): 8.42 сек
Ускорение: 5.37x
```

### Интерпретация результатов

#### Узкие места

1. **API Rate Limit**
   - Если при увеличении потоков появляются ошибки 429 - API ограничивает запросы
   - Решение: использовать кеширование, добавить задержки между запросами

2. **CPU Bound**
   - Если ускорение линейное с количеством потоков - узкое место в CPU
   - Решение: оптимизация алгоритмов стратегий

3. **I/O Bound**
   - Если ускорение меньше количества потоков - узкое место в I/O (диск/сеть)
   - Решение: асинхронные запросы, оптимизация чтения файлов

#### Рекомендации

- **Для небольшого количества сигналов (< 10)**: последовательный режим достаточен
- **Для среднего количества (10-50)**: параллельный режим с 4 потоками
- **Для большого количества (> 50)**: параллельный режим с 8-10 потоками (с учетом rate limits API)

### Настройка тестов

Для изменения параметров тестов отредактируйте `tests/test_performance.py`:

```python
tests = [
    {
        "name": "Тест №1: Последовательный режим",
        "parallel": False,
        "max_workers": 1,
    },
    # Добавьте свои тесты
]
```

### Требования

- Файл `signals/test_signals.csv` с тестовыми сигналами
- Конфигурация `config/backtest_example.yaml`
- Доступ к GeckoTerminal API (или кешированные данные)

---

## 📈 Результаты после внедрения

### Преимущества

1. **Эффективное использование кеша**
   - Повторные запросы не нагружают API
   - Быстрая обработка уже загруженных данных

2. **Понимание производительности**
   - Знание узких мест системы
   - Оптимальные настройки для разных объемов данных

3. **Устойчивость к ошибкам**
   - Fallback на кеш при ошибках API
   - Продолжение работы даже при проблемах с сетью

### Следующие шаги

1. Оптимизация дозагрузки (только недостающие части)
2. Настройка rate limiting для API
3. Мониторинг использования кеша
4. Автоматическая очистка старых кешей

---

*Документация обновлена: 2025-01-XX*





