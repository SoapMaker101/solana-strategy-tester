# 🎉 Release Notes: Phase 4+ - Trade Features & Export Improvements

**Дата релиза:** 2025-12-14  
**Версия:** Phase 4+  
**Статус:** ✅ Ready for Production

---

## 📦 Что нового

### ✨ Trade Features

Каждая сделка теперь включает расширенные метрики для анализа:

- **Market Cap Proxy:**
  - `entry_mcap_proxy` — market cap на входе (price × supply)
  - `exit_mcap_proxy` — market cap на выходе
  - `mcap_change_pct` — изменение market cap в процентах
  - `total_supply_used` — использованное значение supply (из Signal.extra или default 1B)

- **Volume & Volatility Windows (5m/15m/60m):**
  - `vol_sum_5m`, `vol_sum_15m`, `vol_sum_60m` — сумма объёмов до входа
  - `range_pct_5m`, `range_pct_15m`, `range_pct_60m` — диапазон цен (max-min)/entry_price
  - `volat_5m`, `volat_15m`, `volat_60m` — волатильность (std of returns)

**Особенности:**
- ✅ Нет data leakage — окна берутся строго ДО entry_time
- ✅ Не требуют внешних API
- ✅ Доступны в `StrategyOutput.meta` для всех стратегий

---

### 📊 Trades Table Export

Новая возможность экспорта всех сделок в единую CSV-таблицу:

**Автоматический экспорт:**
- Файлы сохраняются как `{strategy}_trades.csv` в `output/reports/`
- Генерируется автоматически после каждого бэктеста

**Структура таблицы:**
- Базовые поля: signal_id, contract_address, entry_time, exit_time, prices, pnl, reason
- Расплющенный meta: все ключи верхнего уровня с префиксом `meta_`
- Вложенные структуры: преобразуются в JSON strings

**Использование:**
```python
# Автоматически вызывается в main.py после бэктеста
reporter.save_trades_table(strategy_name, results)
```

---

### 🛡️ Reset Policy Tests

Защитные тесты гарантируют правильную архитектуру:

- ✅ Стратегии (RR, RRD, Runner) **не устанавливают** reset-флаги в meta
- ✅ Reset-флаги (`triggered_reset`, `closed_by_reset`) появляются **только** в `Position.meta` после PortfolioEngine
- ✅ Четкое разделение ответственности: reset — исключительно портфельная политика

---

## 📝 Обновления документации

- ✅ **CHANGELOG.md** — полное описание всех изменений от 2025-12-14
- ✅ **README.md** — обновлена структура проекта, добавлены разделы о новых функциях
- ✅ **TECHNICAL_REPORT.md** — обновлена версия и структура
- ✅ **PROJECT_ANALYSIS.md** — актуализирован статус проекта

---

## 🧪 Тесты

**Добавлено 19 новых тестов:**

- `tests/test_trade_features.py` — 10 тестов для trade features
- `tests/test_reporter_trades_table.py` — 5 тестов для trades table export
- `tests/test_reset_policy_is_portfolio_only.py` — 4 теста для reset policy

**Итого: 68 тестов, все проходят** ✅

---

## 📁 Измененные файлы

### Новые файлы:
- `backtester/domain/trade_features.py`
- `tests/test_trade_features.py`
- `tests/test_reporter_trades_table.py`
- `tests/test_reset_policy_is_portfolio_only.py`

### Модифицированные файлы:
- `backtester/domain/rr_strategy.py`
- `backtester/domain/rrd_strategy.py`
- `backtester/domain/runner_strategy.py`
- `backtester/infrastructure/reporter.py`
- `main.py`
- `README.md`
- `docs/CHANGELOG.md`
- `docs/TECHNICAL_REPORT.md`
- `docs/PROJECT_ANALYSIS.md`

---

## 🔧 Технические детали

### Trade Features

**Модуль:** `backtester/domain/trade_features.py`

**Функции:**
- `get_total_supply(signal, default=1_000_000_000)` — извлекает supply из Signal.extra
- `calc_mcap_proxy(price, supply)` — вычисляет market cap proxy
- `calc_window_features(candles, entry_time, entry_price, windows_min)` — вычисляет объёмные и волатильностные фичи
- `calc_trade_mcap_features(entry_price, exit_price, total_supply)` — вычисляет mcap features

**Интеграция:**
- Все стратегии автоматически добавляют trade features в `StrategyOutput.meta`
- Фичи доступны для анализа в trades table export

### Trades Table Export

**Метод:** `Reporter.save_trades_table(strategy_name, results, out_name=None)`

**Особенности:**
- Фильтрует только валидные сделки (entry_time != None, reason != no_entry/error)
- Расплющивает meta контролируемо (только верхний уровень)
- Вложенные dict/list → JSON string для сохранения структуры

### Reset Policy

**Принцип:** Reset-политика — исключительно функция PortfolioEngine

**Тесты:**
- Защитные тесты проверяют, что стратегии не устанавливают флаги
- Интеграционный тест проверяет, что флаги появляются только на уровне портфеля

---

## 🚀 Как использовать

### Trade Features

Фичи автоматически добавляются в каждую сделку. Доступны в:

```python
result = strategy.on_signal(data)
print(result.meta["entry_mcap_proxy"])  # Market cap на входе
print(result.meta["vol_sum_5m"])        # Объём за 5 минут до входа
print(result.meta["volat_15m"])         # Волатильность за 15 минут
```

### Trades Table Export

Таблицы создаются автоматически после бэктеста. Находите их в:

```
output/reports/{strategy_name}_trades.csv
```

Открывайте в Excel, pandas, или любом другом инструменте для анализа.

---

## ⚠️ Breaking Changes

**Нет breaking changes.** Все изменения обратно совместимы.

---

## 🔮 Будущие улучшения

- Более гибкая настройка окон для volume/volatility features
- Дополнительные фичи (технические индикаторы)
- Оптимизация производительности расчета features
- Интерактивная визуализация trades table

---

## 📚 Дополнительная информация

- Полная документация: `docs/CHANGELOG.md`
- Технический отчет: `docs/TECHNICAL_REPORT.md`
- Анализ проекта: `docs/PROJECT_ANALYSIS.md`

---

---

## 🎉 Release Notes: Report Pack v1.10

**Дата релиза:** 2025-12-28  
**Версия:** v1.10  
**Статус:** ✅ Ready for Production

---

## 📦 Что нового в v1.10

### ✨ Report Pack (report_pack.xlsx)

Единый XLSX-отчёт со всеми ключевыми таблицами пайплайна в одном файле.

**Расположение:** `output/reports/report_pack.xlsx`

**Листы:**
- `summary` — метаданные запуска и топлайновые метрики
- `positions` — копия `portfolio_positions.csv`
- `portfolio_events` — копия `portfolio_events.csv`
- `stage_a_stability` — копия `strategy_stability.csv` (если есть)
- `stage_b_selection` — копия `strategy_selection.csv` (если есть)
- `policy_summary` — копия `portfolio_policy_summary.csv` (если есть)

**Особенности:**
- ✅ **Best-effort**: отсутствие CSV не ломает генерацию (листы пропускаются)
- ✅ **Graceful fallback**: при отсутствии Excel engine — warning + продолжение, CSV остаются
- ✅ **CSV остаются**: XLSX дополняет, не заменяет CSV (source of truth)
- ✅ **Нормализация datetime**: tz-aware datetime конвертируются в tz-naive для Excel

---

## ✅ Чеклист проверки report_pack.xlsx

### 1. Открыть report_pack.xlsx

```bash
# Проверить наличие файла
ls output/reports/report_pack.xlsx
```

### 2. Проверить листы

Откройте файл в Excel/LibreOffice и проверьте наличие листов:

- [ ] `summary` — метаданные запуска
- [ ] `positions` — позиции (копия `portfolio_positions.csv`)
- [ ] `portfolio_events` — события портфеля (копия `portfolio_events.csv`)
- [ ] `stage_a_stability` — результаты Stage A (если запускался Stage A)
- [ ] `stage_b_selection` — результаты Stage B (если запускался Stage B)
- [ ] `policy_summary` — сводка по политикам (если существует)

**Примечание:** Некоторые листы могут отсутствовать, если соответствующие CSV не были созданы (best-effort подход).

### 3. Проверить данные

- [ ] Лист `summary` содержит метаданные запуска
- [ ] Лист `positions` содержит те же данные, что и `portfolio_positions.csv`
- [ ] Лист `portfolio_events` содержит те же данные, что и `portfolio_events.csv`
- [ ] Количество строк совпадает с CSV (если CSV доступны)

### 4. Проверить source of truth

**Важно:** XLSX — не источник истины, source of truth — CSV.

- [ ] CSV файлы остались на месте (`portfolio_positions.csv`, `portfolio_events.csv`, и т.д.)
- [ ] CSV файлы содержат актуальные данные
- [ ] XLSX создан из CSV (не пересчитан)

### 5. Проверить graceful fallback

Если Excel engine не установлен:

- [ ] В консоли выведено предупреждение (warning)
- [ ] CSV файлы созданы как обычно
- [ ] XLSX файл не создан (или создан с ошибкой, но пайплайн не упал)

---

## 🔧 Технические детали v1.10

### Инварианты

1. **CSV остаются**: XLSX дополняет, не заменяет CSV-выгрузки
2. **Best-effort**: Отсутствующие CSV не ломают генерацию
3. **Graceful fallback**: При отсутствии openpyxl/xlsxwriter — warning + продолжение
4. **Single source of truth**: XLSX собирается из уже созданных CSV

### Поддержка Excel engines

1. **openpyxl** (предпочтительно) — используется первым
2. **xlsxwriter** (fallback) — используется если openpyxl недоступен
3. **Warning** — если оба недоступны, XLSX пропускается

### Нормализация datetime

- tz-aware datetime конвертируются в tz-naive (UTC-naive) перед записью в Excel
- Excel не поддерживает timezone-aware datetimes

---

## 📝 Измененные файлы v1.10

**Новые:**
- `backtester/infrastructure/reporting/report_pack.py` — модуль сборки XLSX
- `backtester/infrastructure/xlsx_writer.py` — утилита для записи XLSX
- `tests/infrastructure/test_xlsx_writer.py` — тесты для XLSX writer
- `tests/infrastructure/test_report_pack_xlsx.py` — тесты для report pack

**Измененные:**
- `backtester/infrastructure/reporter.py` — метод `save_report_pack_xlsx()`
- `main.py` — интеграция с конфигом
- `backtester/research/strategy_stability.py` — XLSX экспорт для Stage A
- `backtester/decision/strategy_selector.py` — XLSX экспорт для Stage B
- `README.md` — раздел "Run v1.10 (quick)"
- `docs/CHANGELOG.md` — запись v1.10

---

## 🧪 Тесты v1.10

**Добавлены:**
- `test_xlsx_created` — проверка создания XLSX
- `test_xlsx_data_integrity` — проверка целостности данных
- `test_xlsx_sheet_names` — проверка имен листов
- `test_report_pack_created_when_openpyxl_available` — позитивный кейс
- `test_report_pack_best_effort_missing_optional_csv` — best-effort поведение
- `test_report_pack_skips_when_no_engine` — graceful fallback

---

**Спасибо за использование Solana Strategy Tester! 🚀**















