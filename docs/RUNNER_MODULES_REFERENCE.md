# Runner Modules & Scripts Reference

**Версия:** 2.1  
**Дата:** 2025-01-XX  
**Обновлено:** 
- Добавлены Stage B критерии и portfolio-level reset
- Исправлен расчет total_capital в fixed mode для множества мелких позиций

---

## 📋 Содержание

1. [Полный список модулей и скриптов](#полный-список-модулей-и-скриптов)
2. [Описание каждого модуля](#описание-каждого-модуля)
3. [Зависимости между модулями](#зависимости-между-модулями)
4. [Workflow использования](#workflow-использования)

---

## Полный список модулей и скриптов

### 🎯 Core Domain Layer (Основная логика)

#### Конфигурация и модели
- **`backtester/domain/runner_config.py`**
  - `RunnerTakeProfitLevel` — dataclass для уровня TP (xn, fraction)
  - `RunnerConfig` — конфигурация Runner стратегии (наследуется от `StrategyConfig`)
  - `create_runner_config_from_dict()` — парсинг из YAML

#### Core логика Runner Ladder
- **`backtester/domain/runner_ladder.py`**
  - `RunnerTradeResult` — результат симуляции одной сделки
  - `RunnerLadderEngine` — независимый движок симуляции Runner Ladder
  - `RunnerLadderEngine.simulate()` — статический метод симуляции

#### Strategy интеграция
- **`backtester/domain/runner_strategy.py`**
  - `RunnerStrategy` — реализация интерфейса `Strategy` для Runner
  - Интеграция с `RunnerLadderEngine`
  - Конвертация `RunnerTradeResult` → `StrategyOutput`

#### Portfolio интеграция
- **`backtester/domain/portfolio.py`**
  - `PortfolioEngine._process_runner_partial_exits()` — обработка частичных выходов
  - Portfolio-level reset логика (equity-based)
  - Поддержка Runner метаданных в `Position.meta`

---

### 🔬 Research Layer (Исследовательские модули)

#### XN Analysis (теоретический анализ потенциала)
- **`backtester/research/xn_analysis/xn_analyzer.py`**
  - `XNAnalyzer` — анализ максимального потенциала роста сигналов
  - Расчет XN-множителей (x2, x5, x10 и т.д.)

- **`backtester/research/xn_analysis/xn_models.py`**
  - `XNAnalysisConfig` — конфигурация XN-анализа
  - `XNSignalResult` — результат анализа одного сигнала

- **`backtester/research/xn_analysis/xn_runner.py`**
  - CLI скрипт для запуска XN-анализа
  - Генерация `xn_per_signal.csv` и `xn_summary.csv`

- **`backtester/research/xn_analysis/__init__.py`**
  - Экспорт публичных классов и функций

#### Stage A/B интеграция
- **`backtester/research/strategy_stability.py`**
  - `calculate_runner_metrics()` — вычисление Runner-метрик из trades CSV
  - `is_runner_strategy()` — определение типа стратегии
  - Интеграция Runner метрик в `build_stability_table()`

- **`backtester/research/window_aggregator.py`**
  - Агрегация сделок по временным окнам
  - Поддержка Runner стратегий в агрегации

- **`backtester/research/run_stage_a.py`**
  - CLI скрипт для запуска Stage A
  - Генерация `strategy_stability.csv` с Runner метриками

---

### 🎯 Decision Layer (Критерии отбора)

#### Selection Criteria
- **`backtester/decision/selection_rules.py`**
  - `SelectionCriteria` — расширенный dataclass с Runner критериями
  - `DEFAULT_CRITERIA` — критерии для RR/RRD стратегий
  - `DEFAULT_RUNNER_CRITERIA` — критерии для Runner стратегий

- **`backtester/decision/strategy_selector.py`**
  - `is_runner_strategy()` — определение типа стратегии
  - `check_strategy_criteria()` — условная проверка критериев
  - `select_strategies()` — применение критериев отбора

- **`backtester/decision/run_stage_b.py`**
  - CLI скрипт для запуска Stage B
  - Отбор стратегий по Runner критериям

---

### 🧪 Tests (Тесты)

#### Domain тесты
- **`tests/domain/test_runner_ladder.py`**
  - Unit-тесты для `RunnerLadderEngine`
  - Тесты частичных выходов, time stop, сортировки уровней

- **`tests/domain/test_runner_strategy.py`**
  - Unit-тесты для `RunnerStrategy`
  - Проверка интеграции с `RunnerLadderEngine`

#### Portfolio тесты
- **`tests/portfolio/test_portfolio_runner_partial_exits.py`**
  - Тесты частичных выходов в портфеле
  - Проверка уменьшения экспозиции, fees, dynamic allocation

- **`tests/portfolio/test_portfolio_runner_reset.py`**
  - Тесты старой логики reset (на уровне позиций)
  - ⚠️ Устарело: заменено на portfolio-level reset

- **`tests/portfolio/test_portfolio_runner_reset_portfolio_level.py`**
  - Тесты portfolio-level reset
  - Проверка закрытия всех позиций при достижении порога equity

#### Research тесты
- **`tests/research/xn_analysis/test_xn_analyzer.py`**
  - Тесты XN-анализа

---

### 📝 Configuration & Documentation

#### Конфигурация
- **`config/strategies_example.yaml`**
  - Пример конфигурации Runner стратегии с baseline параметрами

- **`docs/runner_config_example.yaml`**
  - Документация по конфигурации Runner

- **`config/backtest_example.yaml`**
  - Параметры portfolio-level reset (`runner_reset_enabled`, `runner_reset_multiple`)

#### Документация
- **`docs/XN_RUNNER_RESEARCH.md`**
  - Документация по XN-анализу (research инструмент)

- **`docs/RUNNER_MODULES_REFERENCE.md`** (этот файл)
  - Полный список модулей и скриптов Runner

- **`backtester/research/Пайплайн реализации Runner.txt`**
  - План реализации Runner (исторический документ)

---

### 🔧 Application Layer (Интеграция)

- **`backtester/application/runner.py`**
  - `BacktestRunner` — основной оркестратор
  - Загрузка Runner стратегий из YAML
  - Интеграция с PortfolioEngine

- **`main.py`**
  - CLI точка входа
  - Парсинг Runner конфигурации
  - Генерация `portfolio_summary.csv` с Runner метриками

---

## Описание каждого модуля

### Core Domain Layer

#### `runner_config.py`
**Назначение:** Определение конфигурационных моделей для Runner стратегии.

**Ключевые классы:**
- `RunnerTakeProfitLevel`: Уровень TP с `xn` (множитель) и `fraction` (доля позиции)
- `RunnerConfig`: Полная конфигурация Runner, наследуется от `StrategyConfig`

**Baseline параметры:**
- `take_profit_levels`: `[(2.0, 0.4), (5.0, 0.4), (10.0, 0.2)]`
- `time_stop_minutes`: `20160` (14 дней) или `30240` (21 день)
- `use_high_for_targets`: `True`
- `exit_on_first_tp`: `False`
- `allow_partial_fills`: `True`

#### `runner_ladder.py`
**Назначение:** Независимая core-логика симуляции Runner Ladder стратегии.

**Ключевые классы:**
- `RunnerTradeResult`: Результат симуляции одной сделки
  - `realized_multiple`: Итоговый multiple (например, 3.2x)
  - `levels_hit`: Dict[xn → hit_time]
  - `fractions_exited`: Dict[xn → fraction]
  - `reason`: "time_stop" | "all_levels_hit" | "no_data"

- `RunnerLadderEngine`: Статический класс с методом `simulate()`
  - Сортирует уровни по `xn`
  - Итерирует свечи, проверяет достижение уровней
  - Закрывает части позиции на каждом уровне
  - Обрабатывает time_stop

**Особенности:**
- Не зависит от PortfolioEngine
- Не учитывает комиссии/slippage
- Работает только с ценами и временем

#### `runner_strategy.py`
**Назначение:** Адаптер между `RunnerLadderEngine` и интерфейсом `Strategy`.

**Ключевые методы:**
- `on_signal()`: Принимает `StrategyInput`, возвращает `StrategyOutput`
- `_candles_to_dataframe()`: Конвертация `List[Candle]` → `pd.DataFrame`
- `_ladder_result_to_strategy_output()`: Конвертация `RunnerTradeResult` → `StrategyOutput`

**Метаданные в `StrategyOutput.meta`:**
- `runner_ladder: True`
- `levels_hit`: Dict[str, str] (ISO timestamps)
- `fractions_exited`: Dict[str, float]
- `realized_multiple`: float
- `time_stop_triggered`: bool

#### `portfolio.py` (Runner интеграция)
**Назначение:** Обработка Runner позиций в портфеле.

**Ключевые методы:**
- `_process_runner_partial_exits()`: Обработка частичных выходов
  - Парсит `levels_hit` и `fractions_exited` из `Position.meta`
  - Применяет slippage и fees к каждому частичному выходу
  - Обновляет `balance` и уменьшает `pos.size`
  - Обрабатывает time_stop остаток

- Portfolio-level reset логика:
  - Проверка: `equity >= cycle_start_equity * runner_reset_multiple`
  - Закрытие всех открытых позиций при достижении порога
  - Обновление `cycle_start_equity` после reset

**Метаданные в `Position.meta` для Runner:**
- `runner_ladder: True`
- `original_size`: Исходный размер позиции
- `levels_hit`, `fractions_exited`: Данные из `StrategyOutput.meta`
- `partial_exits`: Список всех частичных выходов с деталями
- `closed_by_reset`: True если закрыта по portfolio-level reset

---

### Research Layer

#### `xn_analysis/` модули
**Назначение:** Теоретический анализ потенциала роста сигналов.

**Workflow:**
1. XN-анализ показывает, какой % сигналов достигает x2, x5, x10
2. На основе результатов выбираются параметры Runner стратегии
3. Запускается полный бэктест с Runner стратегией

**Результаты:**
- `xn_per_signal.csv`: Детальные результаты по каждому сигналу
- `xn_summary.csv`: Агрегированная статистика по XN-уровням

#### `strategy_stability.py` (Runner метрики)
**Назначение:** Вычисление Runner-специфичных метрик для Stage A.

**Функция `calculate_runner_metrics()`:**
- `hit_rate_x2`: Доля сделок, достигших x2
- `hit_rate_x5`: Доля сделок, достигших x5
- `p90_hold_days`: 90-й перцентиль времени удержания позиции
- `tail_contribution`: Доля PnL от top 5% сделок
- `max_drawdown_pct`: Максимальная просадка из `portfolio_summary.csv`

**Интеграция:**
- Автоматически вызывается для Runner стратегий в `build_stability_table()`
- Метрики добавляются в `strategy_stability.csv`

---

### Decision Layer

#### `selection_rules.py`
**Назначение:** Определение критериев отбора стратегий.

**`DEFAULT_RUNNER_CRITERIA`:**
- `min_hit_rate_x2: 0.30` (30%)
- `min_hit_rate_x5: 0.10` (10%)
- `min_tail_contribution: 0.3` (30%)
- `max_drawdown_pct: -0.5` (-50%)
- `min_p90_hold_days: None` (не ограничено)
- `max_p90_hold_days: None` (не ограничено)

#### `strategy_selector.py`
**Назначение:** Применение критериев отбора к стратегиям.

**Логика:**
- Определяет тип стратегии через `is_runner_strategy()`
- Для Runner применяет `DEFAULT_RUNNER_CRITERIA`
- Для RR/RRD применяет `DEFAULT_CRITERIA`

---

## Зависимости между модулями

```
┌─────────────────────────────────────────────────────────────┐
│                    Application Layer                        │
│  main.py → BacktestRunner → PortfolioEngine                │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      Domain Layer                            │
│                                                              │
│  runner_config.py                                           │
│       │                                                      │
│       ├─→ runner_ladder.py (независимый)                   │
│       │                                                      │
│       └─→ runner_strategy.py                                │
│              │                                               │
│              └─→ portfolio.py (Runner интеграция)            │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    Research Layer                            │
│                                                              │
│  xn_analysis/ (теоретический анализ)                        │
│       │                                                      │
│       └─→ strategy_stability.py (Runner метрики)            │
│              │                                               │
│              └─→ window_aggregator.py                        │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    Decision Layer                            │
│                                                              │
│  selection_rules.py (Runner критерии)                        │
│       │                                                      │
│       └─→ strategy_selector.py (условная проверка)           │
│              │                                               │
│              └─→ run_stage_b.py (CLI)                        │
└─────────────────────────────────────────────────────────────┘
```

---

## Workflow использования

### 1. Research Phase (XN Analysis)

```bash
# Запуск XN-анализа для понимания потенциала
python -m backtester.research.xn_analysis.xn_runner \
  --signals signals/example_signals.csv \
  --holding-days 90 \
  --output-dir output/xn_analysis/
```

**Результат:** `xn_summary.csv` показывает, какой % сигналов достигает x2, x5, x10.

### 2. Configuration Phase

На основе XN-анализа создайте Runner стратегию в `config/strategies_example.yaml`:

```yaml
- name: Runner_Baseline
  type: RUNNER
  params:
    take_profit_levels:
      - { xn: 2.0, fraction: 0.4 }
      - { xn: 5.0, fraction: 0.4 }
      - { xn: 10.0, fraction: 0.2 }
    time_stop_minutes: 20160  # 14 дней
    use_high_for_targets: true
```

### 3. Backtest Phase

```bash
# Запуск полного бэктеста с Runner стратегией
python main.py \
  --signals signals/example_signals.csv \
  --strategies-config config/strategies_example.yaml \
  --backtest-config config/backtest_example.yaml
```

**Результат:**
- `*_trades.csv` с метаданными Runner (levels_hit, fractions_exited)
- `portfolio_summary.csv` с Runner метриками (reset_count, cycle_start_equity)

### 4. Stage A (Aggregation)

```bash
# Агрегация по окнам и вычисление Runner метрик
python -m backtester.research.run_stage_a \
  --reports-dir output/reports/
```

**Результат:** `strategy_stability.csv` с Runner метриками:
- `hit_rate_x2`, `hit_rate_x5`
- `p90_hold_days`
- `tail_contribution`
- `max_drawdown_pct`

### 5. Stage B (Selection)

```bash
# Отбор стратегий по Runner критериям
python -m backtester.decision.run_stage_b \
  --stability-csv output/reports/strategy_stability.csv
```

**Результат:** `strategy_selection.csv` с флагом `passed` для каждой стратегии.

---

## Ключевые особенности реализации

### Portfolio-Level Reset

**Логика:**
- Проверка перед обработкой каждой сделки: `equity >= cycle_start_equity * runner_reset_multiple`
- При достижении порога:
  - Закрываются все открытые позиции по текущей цене
  - Обновляется `cycle_start_equity = new equity`
  - Сбрасывается `equity_peak_in_cycle`
  - Устанавливается `reset_until` для игнорирования входов

**Метрики в `PortfolioStats`:**
- `reset_count`: Количество срабатываний reset
- `last_reset_time`: Время последнего reset
- `cycle_start_equity`: Equity в начале текущего цикла
- `equity_peak_in_cycle`: Пик equity в текущем цикле

### Stage B Критерии для Runner

**Отдельные критерии:**
- Runner стратегии проверяются по Runner критериям
- RR/RRD стратегии проверяются по стандартным критериям
- Автоматическое определение типа стратегии

**Baseline критерии:**
- `min_hit_rate_x2: 0.30` — минимум 30% сделок должны достичь x2
- `min_hit_rate_x5: 0.10` — минимум 10% сделок должны достичь x5
- `min_tail_contribution: 0.3` — минимум 30% PnL от top 5% сделок
- `max_drawdown_pct: -0.5` — максимальная просадка не более 50%

---

## Чек-лист проверки Runner

### ✅ Без портфеля: runner ladder даёт адекватный realized_multiple

**Проверка:**
```bash
# Запуск тестов Runner Ladder
pytest tests/domain/test_runner_ladder.py -v
```

**Ожидаемый результат:**
- Частичные выходы работают корректно
- Time stop закрывает остаток
- `realized_multiple` рассчитывается правильно

### ✅ С портфелем, без reset: equity curve не умирает мгновенно

**Проверка:**
```bash
# Запуск бэктеста с Runner без reset
python main.py --strategies-config config/strategies_example.yaml
# Проверить portfolio_summary.csv: max_drawdown_pct не должен быть критическим
```

**Ожидаемый результат:**
- Equity curve показывает рост с просадками
- Tail contribution > 0.3 (зависимость от хвоста)
- Частичные выходы уменьшают экспозицию

### ✅ С reset: появляются циклы

**Проверка:**
```yaml
# В config/backtest_example.yaml
portfolio:
  runner_reset_enabled: true
  runner_reset_multiple: 2.0
```

**Ожидаемый результат:**
- `portfolio_summary.csv` содержит `reset_count > 0`
- `last_reset_time` установлен
- `cycle_start_equity` обновляется после каждого reset

### ✅ Stage B: начинает отбирать Runner-настройки

**Проверка:**
```bash
# Запуск Stage B
python -m backtester.decision.run_stage_b
# Проверить strategy_selection.csv: Runner стратегии должны иметь passed=True/False
```

**Ожидаемый результат:**
- Runner стратегии проверяются по Runner критериям
- "Хорошие" стратегии проходят (`hit_rate_x2 >= 0.30`, `hit_rate_x5 >= 0.10`)
- "Мусор" отфильтровывается

---

## Итог

Runner стратегия полностью интегрирована в систему:

1. ✅ **Core логика:** `runner_ladder.py` + `runner_strategy.py`
2. ✅ **Portfolio интеграция:** Частичные выходы + portfolio-level reset
3. ✅ **Research:** XN-анализ для выбора параметров
4. ✅ **Stage A/B:** Runner-метрики и критерии отбора
5. ✅ **Baseline параметры:** Зафиксированы в `DEFAULT_RUNNER_CRITERIA` и `strategies_example.yaml`

Все модули работают вместе для полного цикла: от research до отбора стратегий.





