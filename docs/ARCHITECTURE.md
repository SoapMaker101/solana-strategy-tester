# 🏗️ Архитектурный анализ: Solana Strategy Tester

**Дата создания:** 2025-12-14  
**Версия:** 1.0

---

## 📋 Обзор

Документ описывает архитектуру проекта Solana Strategy Tester: структуру модулей, их назначение, взаимосвязи и ответственность каждого компонента. Проект следует принципам Clean Architecture с четким разделением на слои.

---

## 🎯 Архитектурные принципы

1. **Clean Architecture** — разделение на слои (domain, application, infrastructure)
2. **Dependency Inversion** — зависимость от абстракций, а не от конкретных реализаций
3. **Single Responsibility** — каждый модуль отвечает за одну задачу
4. **Open/Closed** — открыт для расширения, закрыт для модификации

---

## 📂 Структура проекта

```
backtester/
├── application/          # Слой приложения (оркестрация)
├── domain/              # Бизнес-логика (ядро системы)
├── infrastructure/      # Инфраструктурный слой (I/O, внешние зависимости)
├── research/            # Исследовательский слой (анализ устойчивости)
├── decision/            # Слой принятия решений (отбор стратегий)
└── utils/               # Утилиты общего назначения
```

---

## 🎯 Domain Layer (Бизнес-логика)

**Назначение:** Ядро системы, содержит бизнес-логику, независимую от внешних зависимостей.

### `models.py` — Модели данных

**Ответственность:** Определение основных структур данных системы.

**Основные классы:**
- `Signal` — входной сигнал для торговли (id, contract_address, timestamp, source, narrative, extra)
- `Candle` — OHLCV свеча (timestamp, open, high, low, close, volume)
- `StrategyInput` — входные данные для стратегии (signal, candles, global_params)
- `StrategyOutput` — результат работы стратегии (entry_time, entry_price, exit_time, exit_price, pnl, reason, meta)

**Зависимости:** Только стандартная библиотека Python (dataclasses, datetime, typing)

---

### `strategy_base.py` — Базовый класс стратегий

**Ответственность:** Определение интерфейса для всех стратегий.

**Основные классы:**
- `StrategyConfig` — конфигурация стратегии (name, type, params)
- `Strategy` (ABC) — абстрактный базовый класс со методом `on_signal()`

**Использование:** Все конкретные стратегии наследуются от `Strategy`

**Зависимости:** `models.py`

---

### `rr_strategy.py` — RR стратегия

**Ответственность:** Реализация классической Risk/Reward стратегии (вход на первой свече, выход по TP/SL).

**Основные компоненты:**
- `RRStrategy` — класс стратегии
- Использует `rr_utils.py` для общей логики TP/SL

**Параметры:**
- `tp_multiplier` — множитель для Take Profit
- `sl_multiplier` — множитель для Stop Loss
- `timeout_minutes` — максимальное время удержания позиции

**Зависимости:** `strategy_base.py`, `rr_utils.py`, `trade_features.py`

---

### `rrd_strategy.py` — RRD стратегия

**Ответственность:** Реализация стратегии с отложенным входом по drawdown.

**Основные компоненты:**
- `RRDStrategy` — класс стратегии
- Логика ожидания просадки перед входом
- Использует `rr_utils.py` для логики TP/SL после входа

**Параметры:**
- `drawdown_pct` — процент просадки для входа
- `tp_multiplier`, `sl_multiplier`, `timeout_minutes` — как в RR стратегии

**Зависимости:** `strategy_base.py`, `rr_utils.py`, `trade_features.py`, `price_loader` (для догрузки свечей)

---

### `runner_strategy.py` — Runner стратегия

**Ответственность:** Runner стратегия с лестницей тейк-профитов (ladder strategy).

**Основные компоненты:**
- `RunnerStrategy` — класс стратегии, реализует интерфейс `Strategy`
- Интеграция с `RunnerLadderEngine` для симуляции частичных выходов
- Конвертация `RunnerTradeResult` → `StrategyOutput`

**Ключевые особенности:**
- Несколько уровней TP (например, 2x, 5x, 10x)
- Частичное закрытие позиции на каждом уровне
- Time stop для автоматического закрытия остатка
- Использование HIGH цены как триггера для TP

**Зависимости:** `strategy_base.py`, `runner_ladder.py`, `runner_config.py`, `trade_features.py`

---

### `runner_ladder.py` — Core логика Runner Ladder

**Ответственность:** Независимая симуляция Runner Ladder стратегии (без портфеля).

**Основные компоненты:**
- `RunnerLadderEngine` — статический класс с методом `simulate()`
- `RunnerTradeResult` — результат симуляции одной сделки

**Ключевые особенности:**
- Сортирует уровни TP по `xn` возрастанию
- Итерирует свечи, проверяет достижение уровней
- Закрывает части позиции на каждом уровне
- Обрабатывает time_stop для остатка
- Не зависит от PortfolioEngine (чистая логика)

**Зависимости:** `runner_config.py`, `models.py`

---

### `runner_config.py` — Конфигурация Runner

**Ответственность:** Определение конфигурационных моделей для Runner стратегии.

**Основные компоненты:**
- `RunnerTakeProfitLevel` — dataclass для уровня TP (xn, fraction)
- `RunnerConfig` — полная конфигурация Runner (наследуется от `StrategyConfig`)
- `create_runner_config_from_dict()` — парсинг из YAML

**Baseline параметры:**
- `take_profit_levels`: `[(2.0, 0.4), (5.0, 0.4), (10.0, 0.2)]`
- `time_stop_minutes`: `20160` (14 дней) или `30240` (21 день)
- `use_high_for_targets`: `True`
- `exit_on_first_tp`: `False`
- `allow_partial_fills`: `True`

**Зависимости:** `strategy_base.py`

---

### `rr_utils.py` — Утилиты для RR-стратегий

**Ответственность:** Общая логика для RR и RRD стратегий.

**Основные функции:**
- `apply_tp_sl_logic()` — применение логики TP/SL
- `find_exit()` — поиск точки выхода
- `calculate_atr()` — расчет Average True Range
- `validate_candles()` — проверка качества свечей

**Зависимости:** `models.py`, `warn_dedup.py`

---

### `trade_features.py` — Фичи сделок

**Ответственность:** Расчет дополнительных метрик для сделок.

**Основные функции:**
- `calculate_market_cap_proxy()` — расчет приблизительной капитализации
- `calculate_volume_features()` — объемы в разных временных окнах
- `calculate_volatility_features()` — волатильность в разных окнах
- `add_trade_features()` — добавление всех фич в meta

**Особенности:**
- Фичи рассчитываются только на данных ДО входа (без data leakage)
- Результаты сохраняются в `StrategyOutput.meta`

**Зависимости:** `models.py`

---

### `position.py` — Модель позиции

**Ответственность:** Представление открытой позиции в портфеле.

**Основные классы:**
- `Position` — класс позиции (entry_time, entry_price, size_sol, contract_address, strategy_name)

**Использование:** Используется в `portfolio.py` для отслеживания открытых позиций

**Зависимости:** `models.py`

---

### `portfolio.py` — Портфельный движок

**Ответственность:** Симуляция портфельной торговли с учетом комиссий и ограничений.

**Основные классы:**
- `FeeModel` — модель комиссий (swap_fee_pct, lp_fee_pct, slippage_pct, network_fee_sol)
- `PortfolioConfig` — конфигурация портфеля (initial_balance, allocation_mode, limits, fee_model)
- `PortfolioStats` — статистика портфеля (final_balance, total_return, max_drawdown, trades_executed)
- `PortfolioResult` — результат симуляции (equity_curve, positions, stats)
- `PortfolioEngine` — основной движок симуляции

**Основные методы:**
- `PortfolioEngine.simulate()` — основной метод симуляции
- `PortfolioEngine._position_size()` — расчет размера позиции
- `PortfolioEngine._apply_fees()` — применение комиссий
- `FeeModel.effective_fee_pct()` — расчет эффективных комиссий

**Особенности:**
- Учет всех комиссий (swap, LP, сеть)
- Моделирование проскальзывания
- Портфельные ограничения (max_exposure, max_open_positions)
- **Portfolio-level reset:** Закрытие всех позиций при достижении порога equity
  - Проверка: `equity >= cycle_start_equity * runner_reset_multiple`
  - Обновление `cycle_start_equity` после reset
  - Отслеживание `reset_count`, `last_reset_time`, `equity_peak_in_cycle`
- **Runner частичные выходы:** Обработка частичного закрытия позиций на разных уровнях TP
  - Метод `_process_runner_partial_exits()` обрабатывает каждый уровень
  - Применяет slippage и fees к каждому частичному выходу
  - Уменьшает `open_notional` и увеличивает `balance`

**Зависимости:** `models.py`, `position.py`, `execution_model.py`

---

## 🔧 Application Layer (Слой приложения)

**Назначение:** Оркестрация работы системы, координация между слоями.

### `runner.py` — BacktestRunner

**Ответственность:** Главный оркестратор бэктеста.

**Основные компоненты:**
- `BacktestRunner` — класс оркестратора

**Основные методы:**
- `BacktestRunner.run()` — запуск основного бэктеста
  - Загружает сигналы
  - Для каждого сигнала загружает свечи
  - Применяет все стратегии
  - Собирает результаты
- `BacktestRunner.run_portfolio()` — запуск портфельной симуляции
  - Фильтрует результаты по стратегиям
  - Запускает PortfolioEngine для каждой стратегии
- `BacktestRunner._process_signal()` — обработка одного сигнала (может вызываться параллельно)
- `BacktestRunner._build_portfolio_config()` — построение конфигурации портфеля

**Особенности:**
- Поддержка параллельной обработки сигналов (ThreadPoolExecutor)
- Интеграция с портфельным слоем
- Дедупликация предупреждений через WarnDedup

**Зависимости:**
- `infrastructure.signal_loader` — загрузка сигналов
- `infrastructure.price_loader` — загрузка свечей
- `domain.strategy_base` — стратегии
- `domain.portfolio` — портфельный движок
- `utils.warn_dedup` — дедупликация предупреждений

---

## 🌐 Infrastructure Layer (Инфраструктурный слой)

**Назначение:** Взаимодействие с внешним миром (файлы, API, отчеты).

### `signal_loader.py` — Загрузка сигналов

**Ответственность:** Загрузка сигналов из различных источников.

**Основные классы:**
- `SignalLoader` (ABC) — абстрактный базовый класс
- `CsvSignalLoader` — загрузка из CSV файлов

**Основные методы:**
- `SignalLoader.load_signals()` — абстрактный метод загрузки
- `CsvSignalLoader.load_signals()` — реализация загрузки из CSV

**Особенности:**
- Валидация обязательных полей
- Парсинг timestamp в UTC
- Поддержка extra_json (JSON в CSV колонке)

**Зависимости:** `domain.models` (Signal)

---

### `price_loader.py` — Загрузка свечей

**Ответственность:** Загрузка ценовых данных (свечей) из различных источников.

**Основные классы:**
- `PriceLoader` (ABC) — абстрактный базовый класс
- `CsvPriceLoader` — загрузка из локальных CSV файлов
- `GeckoTerminalPriceLoader` — загрузка через GeckoTerminal API

**Основные методы:**
- `PriceLoader.load_prices()` — абстрактный метод загрузки
- `CsvPriceLoader.load_prices()` — реализация загрузки из CSV
- `GeckoTerminalPriceLoader.load_prices()` — реализация загрузки через API

**Особенности CsvPriceLoader:**
- Фильтрация по временному окну
- Валидация данных свечей
- Сортировка по timestamp

**Особенности GeckoTerminalPriceLoader:**
- Кеширование данных в локальные CSV
- Пагинация для исторических данных
- Retry-логика с экспоненциальной задержкой
- Rate limiting (30 запросов/минуту)
- Умное кеширование (проверка покрытия диапазона)
- Fallback на кеш при ошибках API
- Выбор пула с наибольшей ликвидностью

**Зависимости:** `domain.models` (Candle)

---

### `reporter.py` — Генерация отчетов

**Ответственность:** Генерация различных типов отчетов по результатам бэктеста.

**Основные классы:**
- `Reporter` — класс для генерации отчетов

**Основные методы:**
- `Reporter.generate_full_report()` — генерация полного отчета (JSON, CSV, HTML)
- `Reporter.save_trades_table()` — сохранение единой таблицы сделок
- `Reporter.save_portfolio_results()` — сохранение портфельных результатов
- `Reporter._calculate_metrics()` — расчет метрик (winrate, Sharpe, drawdown, profit factor)
- `Reporter._generate_charts()` — генерация графиков (equity curve, PnL distribution, exit reasons)

**Генерируемые метрики:**
- Winrate
- Sharpe ratio (годовая)
- Max drawdown
- Profit factor
- Средняя/медианная PnL
- Распределение по источникам сигналов
- Распределение по причинам выхода

**Формат отчетов:**
- JSON — структурированные данные
- CSV — табличные данные
- HTML — визуализация с графиками (matplotlib)

**Зависимости:** `domain.models`, `matplotlib`, `pandas`, `numpy`

---

## 🔬 Research Layer (Исследовательский слой)

**Назначение:** Анализ устойчивости стратегий и исследовательская аналитика.

### `window_aggregator.py` — Агрегация по окнам

**Ответственность:** Разбиение сделок на временные окна и расчет метрик для каждого окна.

**Основные функции:**
- `load_trades_csv()` — загрузка таблицы сделок из CSV
- `calculate_window_metrics()` — расчет метрик для окна сделок
- `aggregate_strategy_by_equal_windows()` — агрегация стратегии по равным окнам
- `aggregate_all_strategies()` — агрегация всех стратегий

**Особенности:**
- Поддержка равных временных окон (2, 3, 4, 5 частей)
- Расчет метрик для каждого окна (winrate, total_pnl, median_pnl, max_drawdown, profit_factor)
- Сохранение детальной таблицы всех окон

**Зависимости:** `pandas`, `numpy`, `statistics`

---

### `strategy_stability.py` — Анализ устойчивости

**Ответственность:** Расчет метрик устойчивости стратегий.

**Основные функции:**
- `calculate_stability_metrics()` — расчет метрик устойчивости для одной стратегии
- `build_stability_table()` — построение таблицы устойчивости для всех стратегий
- `generate_stability_table_from_reports()` — генерация таблицы из файлов trades.csv

**Метрики устойчивости:**
- `survival_rate` — доля прибыльных окон (0.0 - 1.0)
- `worst_window_pnl` — худший PnL среди всех окон
- `median_window_pnl` — медианный PnL окон
- `pnl_variance` — дисперсия PnL окон
- `windows_total` — общее количество окон

**Выходные данные:**
- `strategy_stability.csv` — таблица устойчивости всех стратегий
- `stage_a_summary.csv` — детальная таблица со всеми окнами

**Зависимости:** `window_aggregator.py`, `pandas`

---

### `run_stage_a.py` — CLI для Stage A

**Ответственность:** Точка входа для запуска анализа устойчивости.

**Основные функции:**
- `main()` — главная функция CLI
- `format_summary()` — форматирование краткого summary

**Параметры командной строки:**
- `--reports-dir` — директория с файлами trades.csv
- `--splits` — список количеств разбиений (2, 3, 4, 5)

**Зависимости:** `window_aggregator.py`, `strategy_stability.py`

---

### `signal_quality/` — Анализ и фильтрация сигналов

**Ответственность:** Анализ качества сигналов и их фильтрация по market cap proxy для улучшения входных данных бэктестинга.

**Основные модули:**

#### `feature_extractor.py` — Извлечение признаков

**Ответственность:** Вычисление признаков сигналов для анализа качества.

**Основные функции:**
- `load_signals()` — загрузка сигналов из CSV
- `load_candles()` — загрузка свечей для контракта
- `get_entry_price()` — получение цены входа (режимы `t` или `t+1m`)
- `extract_signal_features()` — извлечение всех признаков для DataFrame сигналов

**Вычисляемые признаки:**
- `entry_price` — цена входа (на момент сигнала или t+1m)
- `market_cap_proxy` — прокси market cap (`entry_price × 1_000_000_000`)
- `max_xn` — максимальный множитель цены в окне анализа
- `time_to_xn` — время до достижения уровней (2x, 3x, 5x, 7x, 10x)
- `lived_minutes` — количество минут со свечами после entry
- `status` — статус обработки (`ok`, `no_candles`, `no_entry`, `error`)

**Зависимости:** `infrastructure.signal_loader`, `infrastructure.price_loader`, `domain.models`

---

#### `cap_thresholds.py` — Анализ порогов

**Ответственность:** Анализ различных порогов `min_market_cap_proxy` для выбора оптимального фильтра.

**Основные функции:**
- `analyze_cap_thresholds()` — анализ порогов и вычисление метрик
- `save_cap_threshold_report()` — сохранение отчёта в CSV

**Метрики анализа:**
- `kept_signals` — количество оставшихся сигналов
- `kept_pct` — процент оставшихся сигналов
- `kept_runners` — количество runner'ов, которые остались
- `runner_recall_pct` — процент runner'ов, которые остались
- `non_runner_removed_pct` — процент non-runner'ов, которые были отрезаны
- `runner_share_before`, `runner_share_after` — доля runner'ов до и после фильтрации

**Зависимости:** `pandas`

---

#### `filter_signals.py` — Фильтрация сигналов

**Ответственность:** Фильтрация сигналов по порогам market cap proxy.

**Основные функции:**
- `filter_signals()` — фильтрация сигналов по `min_market_cap_proxy` и `status`
- `save_filtered_signals()` — сохранение отфильтрованных сигналов в CSV
- `generate_filter_summary()` — генерация summary фильтрации
- `save_filter_summary()` — сохранение summary в JSON

**Логика фильтрации:**
- `market_cap_proxy >= min_market_cap_proxy`
- Опционально: `status == "ok"`

**Зависимости:** `infrastructure.signal_loader`, `pandas`

---

#### `run_signal_filter_pipeline.py` — CLI пайплайн

**Ответственность:** Точка входа для запуска полного пайплайна фильтрации сигналов.

**Основные функции:**
- `main()` — главная функция CLI
- `parse_args()` — разбор аргументов командной строки

**Пайплайн:**
1. Загрузка сигналов
2. Извлечение признаков (`feature_extractor.py`)
3. Анализ порогов (`cap_thresholds.py`)
4. Фильтрация сигналов (`filter_signals.py`)
5. Сохранение результатов и summary

**Параметры командной строки:**
- `--signals` — путь к CSV файлу с сигналами (обязательно)
- `--candles-dir` — базовая директория со свечами
- `--timeframe` — таймфрейм свечей (`1m`, `5m`, `15m`)
- `--entry-mode` — режим поиска entry_price (`t` или `t+1m`)
- `--horizon-days` — горизонт анализа в днях
- `--use-high` — использовать high цену для max_xn
- `--runner-xn-threshold` — порог для определения runner'а
- `--thresholds` — список порогов для анализа
- `--min-market-cap-proxy` — минимальный порог для фильтрации (обязательно)
- `--output-dir` — директория для сохранения результатов

**Выходные файлы:**
- `signal_features.csv` — признаки всех сигналов
- `cap_threshold_report.csv` — отчёт по порогам
- `signals_filtered.csv` — отфильтрованные сигналы
- `signal_filter_summary.json` — summary фильтрации

**Зависимости:** `feature_extractor.py`, `cap_thresholds.py`, `filter_signals.py`

**Документация:** `docs/SignalFiltering.md`

---

## ✅ Decision Layer (Слой принятия решений)

**Назначение:** Автоматический отбор стратегий на основе критериев.

### `selection_rules.py` — Правила отбора

**Ответственность:** Определение критериев отбора стратегий.

**Основные классы:**
- `SelectionCriteria` — класс критериев отбора

**Критерии (по умолчанию):**
- `min_survival_rate = 0.5` — минимум 50% окон должны быть прибыльными
- `max_pnl_variance = 0.1` — максимальная дисперсия PnL
- `min_worst_window_pnl = -0.2` — минимум худшего окна (не более 20% потерь)
- `min_median_window_pnl = 0.0` — минимум медианного PnL (неотрицательный)
- `min_windows = 4` — минимум окон для статистической значимости

**Зависимости:** Только стандартная библиотека Python (dataclasses)

---

### `strategy_selector.py` — Логика отбора

**Ответственность:** Применение критериев отбора к стратегиям.

**Основные функции:**
- `check_strategy_criteria()` — проверка стратегии по критериям
- `select_strategies()` — отбор стратегий из таблицы устойчивости
- `generate_selection_table_from_stability()` — генерация таблицы отбора из CSV
- `load_stability_csv()` — загрузка таблицы устойчивости

**Логика отбора:**
- Стратегия проходит, если **ВСЕ** критерии выполнены
- Для непрошедших стратегий фиксируются причины отклонения

**Выходные данные:**
- `strategy_selection.csv` — таблица отбора со всеми стратегиями и результатами проверки

**Зависимости:** `selection_rules.py`, `pandas`

---

### `run_stage_b.py` — CLI для Stage B

**Ответственность:** Точка входа для запуска отбора стратегий.

**Основные функции:**
- `main()` — главная функция CLI
- `format_selection_summary()` — форматирование краткого summary

**Параметры командной строки:**
- `--stability-csv` — путь к файлу strategy_stability.csv
- `--output-csv` — путь для сохранения результатов

**Зависимости:** `strategy_selector.py`, `selection_rules.py`

---

## 🛠️ Utils Layer (Утилиты)

**Назначение:** Вспомогательные функции общего назначения.

### `warn_dedup.py` — Дедупликация предупреждений

**Ответственность:** Потокобезопасная дедупликация повторяющихся предупреждений.

**Основные классы:**
- `WarnDedup` — класс для дедупликации

**Особенности:**
- Потокобезопасность (thread-safe)
- Ограничение количества выводов одинаковых предупреждений
- Поддержка summary статистики

**Использование:** Используется в стратегиях и других модулях для уменьшения шума в логах

**Зависимости:** Только стандартная библиотека Python (threading, collections)

---

## 📊 Схема зависимостей

```
┌─────────────────────────────────────────────────────────────┐
│                    Infrastructure Layer                      │
│  signal_loader  │  price_loader  │  reporter                │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    Application Layer                         │
│                      runner.py                               │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                      Domain Layer                            │
│  models │ strategy_base │ rr_strategy │ rrd_strategy │ ...  │
│  portfolio │ position │ trade_features │ rr_utils            │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    Research Layer                            │
│  window_aggregator │ strategy_stability │ run_stage_a        │
│  signal_quality (feature_extractor, filter_signals, etc.)    │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    Decision Layer                            │
│  selection_rules │ strategy_selector │ run_stage_b          │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                      Utils Layer                             │
│                        warn_dedup                            │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Потоки данных

### Основной бэктест

```
Signals (CSV)
    ↓
CsvSignalLoader
    ↓
List[Signal]
    ↓
BacktestRunner.run()
    ↓
For each Signal:
    PriceLoader.load_prices()
        ↓
    List[Candle]
        ↓
    StrategyInput
        ↓
    Strategy.on_signal()
        ↓
    StrategyOutput
        ↓
List[Results]
    ↓
Reporter.generate_full_report()
    ↓
Reports (JSON, CSV, HTML)
```

### Портфельная симуляция

```
List[Results]
    ↓
PortfolioEngine.simulate()
    ↓
Filter by strategy
    ↓
Apply portfolio constraints
    ↓
Calculate fees
    ↓
PortfolioResult
    ↓
Reporter.save_portfolio_results()
    ↓
Portfolio Reports
```

### Stage A (Устойчивость)

```
{strategy}_trades.csv
    ↓
window_aggregator.load_trades_csv()
    ↓
aggregate_all_strategies()
    ↓
Split into windows (2, 3, 4, 5)
    ↓
Calculate metrics per window
    ↓
strategy_stability.build_stability_table()
    ↓
strategy_stability.csv
```

### Stage B (Отбор)

```
strategy_stability.csv
    ↓
strategy_selector.load_stability_csv()
    ↓
select_strategies()
    ↓
Apply SelectionCriteria
    ↓
Check each criterion
    ↓
strategy_selection.csv (with passed/failed)
```

---

## 🎯 Ответственность модулей (краткое резюме)

| Модуль | Ответственность |
|--------|----------------|
| `domain/models.py` | Определение структур данных |
| `domain/strategy_base.py` | Интерфейс стратегий |
| `domain/rr_strategy.py` | RR стратегия |
| `domain/rrd_strategy.py` | RRD стратегия |
| `domain/runner_strategy.py` | Runner стратегия |
| `domain/rr_utils.py` | Общая логика RR/RRD |
| `domain/trade_features.py` | Расчет фич сделок |
| `domain/position.py` | Модель позиции |
| `domain/portfolio.py` | Портфельная симуляция |
| `application/runner.py` | Оркестрация бэктеста |
| `infrastructure/signal_loader.py` | Загрузка сигналов |
| `infrastructure/price_loader.py` | Загрузка свечей |
| `infrastructure/reporter.py` | Генерация отчетов |
| `research/window_aggregator.py` | Агрегация по окнам |
| `research/strategy_stability.py` | Метрики устойчивости |
| `research/run_stage_a.py` | CLI Stage A |
| `decision/selection_rules.py` | Критерии отбора |
| `decision/strategy_selector.py` | Логика отбора |
| `decision/run_stage_b.py` | CLI Stage B |
| `utils/warn_dedup.py` | Дедупликация предупреждений |

---

## 📚 Дополнительные ресурсы

- **PRODUCT_ANALYSIS.md** — продуктовый анализ
- **TECHNICAL_REPORT.md** — технический отчет
- **PIPELINE_GUIDE.md** — руководство по пайплайну
- **PORTFOLIO_LAYER.md** — детали портфельного слоя

---

*Документ обновлен: 2025-12-14*



