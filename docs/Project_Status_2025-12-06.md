.

📄 Solana Strategy Tester — Статус проекта
Дата: 06.12.2025
1. Общий статус проекта

Проект перешёл из Фазы 1 (структура, подготовка среды) и успешно завершил Фазу 2, включающую:

нормализацию сущностей данных (Signal, Candle);

создание единых интерфейсов для цен и сигналов;

введение универсальных входов/выходов стратегий;

переписывание Runner на новую архитектуру;

корректный запуск стратегий (RR, Runner) и заглушки RRD;

стабильный pipeline: от сигнала до результата торговли.

Система успешно выполняет backtest, возвращая корректные StrategyOutput по каждому сигналу и стратегии.

Бэкэнд проекта теперь архитектурно готов для перехода к Фазе 3
(реализация сложных стратегий, подключение реальных данных, отчётность).

2. Архитектурная основа (сущности Фазы 2)
2.1 Signal

Единый объект входного сигнала.

@dataclass
class Signal:
    id: str
    contract_address: str
    timestamp: datetime
    source: str
    narrative: str
    extra: Dict[str, Any] = field(default_factory=dict)

Назначение:

Описывает событие, запускающее торговое решение.

Нормализует входные данные (CSV, API, Telegram бот).

Исключает ошибки, связанные с DataFrame/строчными ключами.

2.2 Candle

Объект OHLCV-свечи.

@dataclass
class Candle:
    timestamp: datetime
    open: float
    high: float
    low: float
    close: float
    volume: float

Назначение:

Унифицированное представление свечей вне зависимости от источника.

Позволяет стратегиям работать с чистыми Python-объектами.

2.3 StrategyInput
@dataclass
class StrategyInput:
    signal: Signal
    candles: List[Candle]
    global_params: Dict[str, Any]

Назначение:

Единый формат данных, передающихся в стратегию.

Гарантирует, что каждая стратегия получает одинаковую структуру.

2.4 StrategyOutput
@dataclass
class StrategyOutput:
    entry_time: Optional[datetime]
    entry_price: Optional[float]
    exit_time: Optional[datetime]
    exit_price: Optional[float]
    pnl: float
    reason: Literal["tp", "sl", "timeout", "no_entry", "error"]
    meta: Dict[str, Any] = field(default_factory=dict)

Назначение:

Стандартизированное описание результата одной сделки.

Подходит для построения отчётности и сравнения стратегий.

3. Лоадеры данных
3.1 SignalLoader → CsvSignalLoader

Функция: загружает сигналы из CSV и превращает их в List[Signal].

Особенности реализации:

строгая проверка колонок (id, contract_address, timestamp, source, narrative);

автоматическое преобразование timestamp в UTC;

парсинг поля extra_json → словарь.

3.2 PriceLoader → CsvPriceLoader

Функция: загружает свечи из локальных CSV-файлов и конвертирует их в List[Candle].

Особенности:

строгая проверка колонок OHLCV;

нормализация временной зоны;

сортировка по времени;

фильтрация по окну [start_time, end_time].

Ожидаемый формат файла:

data/candles/<contract>_1m.csv

4. Новый Runner (BacktestRunner)

Полностью переписан под новую архитектуру.

Основной pipeline:

Загрузить сигналы (List[Signal]).

Для каждого сигнала загрузить окно свечей → List[Candle].

Сформировать StrategyInput.

Передать в каждую стратегию: strategy.on_signal(input).

Принять StrategyOutput.

Добавить в итоговый список результатов.

Сохранить отчёт в output/reports/backtest_results.csv.

Отличия от старой версии:

отказ от DataFrame как передачи данных;

типизированный контролируемый вход в стратегию;

полный контроль ошибок стратегий;

проект структурирован для расширения.

5. Реализованные стратегии
5.1 RRStrategy (реальный TP/SL)

Реализует:

вход → первая свеча после сигнала;

выход → TP или SL, сканируя свечи;

если ни TP, ни SL → выход по времени (timeout);

PnL вычисляется корректно;

возвращает нормальный StrategyOutput.

🟢 Стратегия полностью рабочая.

5.2 RRDStrategy (заглушка)

пока повторяет поведение Runner/timeout;

архитектурно совместима с StrategyInput / StrategyOutput;

в Фазе 3 будет реализован реальный drawdown-entry.

5.3 RunnerStrategy

Минимальная стратегия:

вход по первой свече;

выход по последней свече окна;

полезна для sanity-check и отладки данных.

6. Тестовые данные и успешный прогон

Свечи созданы через generate_fake_candles.py.
Сигналы — через signals/example_signals.csv.

Пример вывода (фрагмент):

RR: timeout → pnl = +1.03%
RR: tp → pnl = +10%
RRD: timeout → pnl ~1%


Это подтверждает:

RR TP/SL логика работает корректно,

окно свечей подаётся корректно,

сигналы отражены правильно,

стратегия работает с реальными свечами.

7. Что осталось на Фазу 2.5 и Фазу 3
7.1 Фаза 2.5 — Определение источника данных

Варианты:

DexScreener API

GMGN API

Birdeye API

локальный CSV генератор

Задачи фазы:

Выбрать основной источник свечей.

Написать API-адаптер GmgnPriceLoader / DexPriceLoader.

Добавить кэширование свечей в data/candles/requested/.

Гарантировать одинаковые данные при повторном запуске бэктеста.

7.2 Фаза 3 — Стратегии и аналитика
Стратегии

Реальный RRD (drawdown-entry).

Multi-entry / multi-exit.

Комбинации стратегий.

Отчётность

Winrate, AvgPnL, MedianPnL.

Distribution by signal source.

Drawdown analysis.

Equity curve.

CSV/HTML/PDF отчёты.

8. Текущее дерево проекта (06.12.2025)
project/
│
├── backtester/
│   ├── runner.py
│   ├── models.py
│   ├── signal_loader.py
│   ├── price_loader.py
│   └── strategy/
│       ├── base.py
│       ├── rr_strategy.py
│       ├── rrd_strategy.py
│       └── runner_strategy.py
│
├── data/
│   └── candles/
│       └── TESTTOKEN_1m.csv
│
├── signals/
│   └── example_signals.csv
│
├── output/
│   └── reports/
│       └── backtest_results.csv
│
├── config/
│   ├── backtest_example.yaml
│   └── strategies_example.yaml
│
├── generate_fake_candles.py
├── main.py
└── requirements.txt

9. Вывод

По состоянию на 06.12.2025:

✔ Архитектура бэктестера полностью приведена к современному виду
✔ Все базовые сущности нормализованы
✔ Стратегии используют типизированные входы/выходы
✔ RR работает корректно (TP/SL)
✔ Pipeline стабилен и расширяем
✔ Проект готов к реальной разработке стратегий